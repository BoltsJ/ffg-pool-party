{
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "package": "cd dist && zip -r module.zip *",
    "check": "tsc --noEmit",
    "format": "prettier --write src/",
    "clean": "rm -fr dist && rm -fr node_modules"
  },
  "devDependencies": {
    "@league-of-foundry-developers/foundry-vtt-types": "^0.8.9-6",
    "@rollup/plugin-typescript": "^8.2.5",
    "prettier": "^2.4.1",
    "rollup": "^2.58.0",
    "rollup-plugin-copy": "^3.4.0",
    "rollup-plugin-terser": "^7.0.2",
    "tslib": "^2.3.1",
    "typescript": "^4.4.4"
  }
}
